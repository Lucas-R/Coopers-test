export type ButtonProps = {
  label: string,
  maxWidth?: number,
  bgColor?: string,
  onClick: () => void,
}

export type ButtonStyleProps = {
  maxWidth?: number,
  height?: number
  bg: string | undefined,
}
